# Jarvis memory for DeepSeek Harness

`@jarvis/dsh-memory` retrieves source-scoped Jarvis context before each DSH model step. Jarvis remains the memory owner; optional automatic capture creates review-required candidates only.
